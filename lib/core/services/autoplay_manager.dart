class AutoPlayManager {
  AutoPlayManager._();

  /// Flag indicating whether FeedScreen should automatically start auto-play
  /// because the user initiated auto-play in CampaignScreen.
  static bool shouldAutoStartFeedAutoPlay = false;
}
